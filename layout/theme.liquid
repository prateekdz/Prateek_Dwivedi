<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ page_title | escape }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ 'gift-guide.css' | asset_url }}">
    {{ content_for_header }}
  </head>
  <body class="gift-guide-page" data-template="page.gift-guide">
    <main id="MainContent" role="main" data-template="page.gift-guide">
      {{ content_for_layout }}
    </main>
    <script src="{{ 'gift-guide.js' | asset_url }}" defer></script>
  </body>
</html>
