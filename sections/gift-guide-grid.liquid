<section id="gift-guide-grid" class="gift-guide-grid" aria-label="Gift Guide Products" data-gg-grid>
  <div class="gift-guide-grid__container">
    <div class="gift-guide-grid__intro">
      <p class="gift-guide-grid__eyebrow">Gift Guide</p>
      <h2 class="gift-guide-grid__heading">{{ section.settings.section_heading }}</h2>
      <p class="gift-guide-grid__description">{{ section.settings.section_description | newline_to_br }}</p>
    </div>

    <div class="gift-guide-grid__cards">
      {% for block in section.blocks %}
        {% assign hotspot_class = '' %}
        {% if block.settings.hotspot_position == 'top-left' %}
          {% assign hotspot_class = 'gift-guide-card--hotspot-left' %}
        {% elsif block.settings.hotspot_position == 'center-right' %}
          {% assign hotspot_class = 'gift-guide-card--hotspot-center-right' %}
        {% elsif block.settings.hotspot_position == 'center' %}
          {% assign hotspot_class = 'gift-guide-card--hotspot-center' %}
        {% else %}
          {% assign hotspot_class = 'gift-guide-card--hotspot-right' %}
        {% endif %}

        <div class="gift-guide-card-wrapper {{ hotspot_class }}">
          {% if block.settings.product != blank %}
            {% assign product_handle = block.settings.product.handle | default: block.settings.product %}
            {% assign product_obj = all_products[product_handle] %}
            {% if product_obj == nil %}
              {% assign product_obj = block.settings.product %}
            {% endif %}

            <button type="button" class="gift-guide-card" data-product-handle="{{ product_handle }}" data-gg-block-id="{{ block.id }}" aria-haspopup="dialog">
              {% if product_obj != nil and product_obj.featured_image != blank %}
                <img class="gift-guide-card__image" src="{{ product_obj.featured_image | image_url: width: 1200 }}" alt="{{ product_obj.title | escape }}" loading="lazy">
              {% else %}
                <img class="gift-guide-card__image" src="{{ 'Frame.svg' | asset_url }}" alt="Placeholder" loading="lazy">
              {% endif %}

              {% if product_obj != nil %}
                <span class="gift-guide-card__sr-only">{{ product_obj.title | escape }} • {{ product_obj.price | money }}</span>
              {% endif %}

              <span class="gift-guide-card__hotspot" aria-label="Quick view">+</span>
              {% if block.settings.show_dot_markers %}
                <span class="gift-guide-card__dot gift-guide-card__dot--a"></span>
                <span class="gift-guide-card__dot gift-guide-card__dot--b"></span>
              {% endif %}
            </button>
          {% else %}
            <div class="gift-guide-card gift-guide-card--empty">
              <span>Select a product in the section settings</span>
            </div>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  </div>

  <div class="gift-guide-modal gift-guide-modal--closed" id="gift-guide-modal" aria-hidden="true" role="dialog" aria-modal="true" aria-labelledby="gift-guide-modal-title" data-gg-modal>
    <div class="gift-guide-modal__overlay" data-gg-close></div>
    <div class="gift-guide-modal__content">
      <button type="button" class="gift-guide-modal__close" aria-label="Close" data-gg-close>✕</button>
      <div class="gift-guide-modal__panel">
        <div class="gift-guide-modal__image-area">
          <img class="gift-guide-modal__image" src="" alt="" loading="lazy">
        </div>
        <div class="gift-guide-modal__details">
          <p class="gift-guide-modal__eyebrow">Quick view</p>
          <h2 id="gift-guide-modal-title" class="gift-guide-modal__title"></h2>
          <p class="gift-guide-modal__price"></p>
          <p class="gift-guide-modal__description"></p>
          <div class="gift-guide-modal__options" data-gg-options></div>
          <button type="button" class="gift-guide-button gift-guide-button--black gift-guide-modal__add" data-gg-add-to-cart disabled>
            <span>ADD TO CART</span>
            <span class="gift-guide-button__arrow" aria-hidden="true">→</span>
          </button>
          <p class="gift-guide-modal__notice" data-gg-notice aria-live="polite"></p>
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Gift Guide Product Grid",
  "settings": [
    { "type": "text", "id": "section_heading", "label": "Heading", "default": "Curated Gifts for Every Moment" },
    { "type": "textarea", "id": "section_description", "label": "Section description", "default": "Explore a handpicked collection of gifts, styled for every taste and occasion." }
  ],
  "blocks": [
    {
      "type": "product_block",
      "name": "Product",
      "settings": [
        { "type": "product", "id": "product", "label": "Product" },
        {
          "type": "select",
          "id": "hotspot_position",
          "label": "Hotspot position",
          "options": [
            { "value": "top-right", "label": "Top right" },
            { "value": "top-left", "label": "Top left" },
            { "value": "center-right", "label": "Center right" },
            { "value": "center", "label": "Center" }
          ],
          "default": "top-right"
        },
        { "type": "checkbox", "id": "show_dot_markers", "label": "Show extra overlay markers", "default": false }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Gift Guide Product Grid",
      "blocks": [
        { "type": "product_block" },
        { "type": "product_block" },
        { "type": "product_block" },
        { "type": "product_block" },
        { "type": "product_block" },
        { "type": "product_block" }
      ]
    }
  ]
}
{% endschema %}
